apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "supabase.component.fullname" (dict "ctx" . "component" "kong") }}-kong-config
  labels:
    {{- include "supabase.labels" . | nindent 4 }}
data:
  kong.yml: {{ .Files.Get "files/api/kong.yml" | quote }}
